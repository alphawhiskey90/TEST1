password = raw.input("Enter your password")

def acceptable_pw_length(password):
if len(password) < 15:
print "Your password does not meet the minimum password length"

elif len(password) > 15:
print "Your password has met the minimum password length"

acceptable_pw_length(password)
